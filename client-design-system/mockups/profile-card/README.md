# Profile Card

A one-off example mockup that shows a few component states without becoming a full app.

This mockup lives in:

```text
client-design-system/mockups/profile-card/
```

After Gallery routing is wired, it should be available at:

```text
/mockup/profile-card
```

Run the Gallery with:

```bash
npm run dev
```
