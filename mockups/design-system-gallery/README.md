# Design System Gallery

A visual inventory of the available design-system pieces.

Run it with:

```bash
npm run gallery
```

Or directly:

```bash
npm run dev design-system-gallery
```

Save screenshots with:

```bash
npm run snapshot:gallery
```

Use this page when a designer wants to see what components, states, colors, and small patterns are available before asking an agent to make a mockup.
